---

- name: vargant
  hosts: vjhvf
  gather_facts: false
  
  tasks:

   - name: cd_vagrant
     win_command: cd C:\HashiCorp\Vagrant\bin\

   - name: add image
     win_command: vagrant.exe box add ubuntu/trusty64

- name: vargant up
  hosts: vjhvf
  gather_facts: false
  
  tasks:
  
   - name: vagrant up
     cmd:  vagrant.exe up ubuntu/trusty64
